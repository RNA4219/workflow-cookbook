---
acceptance_id: AC-20260912-02
task_id: 20260912-02
intent_id: INT-ADOPTION-VALIDATION
owner: workflow-cookbook
status: approved
reviewed_at: 2026-09-12
reviewed_by: workflow-cookbook
approval_type: technical
release_approval_id: null
---

# コピーと導入検査の受入

## Scope

[Task](../tasks/task-adoption-validation-20260912.md) /
[仕様](../contracts/adoption-validation.md)

## Acceptance Criteria

- autocrlfと既存attributesの組合せでcommit/clone後の単独検査が成功する。
- 本文・バイナリ・部分的な改行変更を検出し、旧v1コピーの検査を維持する。
- 空文書・パスの種類違い・破損JSONでTier 3に合格しない。
- 同版だけがドリフト検査に成功し、不明や比較対象ゼロは失敗する。

## Evidence

| 検査 | 結果 |
| --- | --- |
| Git往復 | autocrlfの3値とattributesの3設定、計9ケースでコピー・clone・単独検査・再実行が成功 |
| 内容変更 | 本文、バイナリ、末尾改行削除、部分的な改行変更を検出 |
| Tier | 空、型違い、破損JSONを拒否し、有効な文書構造は成功 |
| 版検査 | 同版、異版、unknown、比較対象ゼロ、未検査を区別。unknownは明示的な検査で失敗 |
| 実ファイル一式 | 固定した577ファイルをGit往復後に検査成功。326件の改行変換を記録済みhashで照合 |
| 初回の配布wheel | 隔離環境へoffline install。6公開CLI、コピー、検査、再実行、clone後の単独検査が成功 |
| 静的検査 | Ruff、mypy（131 source files）、変更コードのBanditが成功 |
| 初回の全体回帰 | 1211件成功、4件skip。行coverage 84.20%（基準80%） |
| 初回の変更モジュール | コピー98.43%、Tier検査92.20%の行coverage |

workspaceの `research/workflow-copy-followup-20260912/` に証跡を保存した。
再現前は `results.json`、修正後の同じ577ファイルの検証は `fixed-e2e/results.json`、
wheelのhashと結果は `wheel-check.json`、全体回帰は `pytest-final.log`、`junit-final.xml`、
`coverage-final.json` を参照する。初回のCLI 5件はuvの環境作成で失敗し、
作業領域内のcacheと必要な依存を準備した再試験では全5件が成功した（`cli-online.log`）。

577ファイルのコピー元は `93552a4d423b2f63f31a3ad30f157b1077874c62` に固定し、
修正したinstallerとwheelを使用した。未コミット差分を上流snapshotに含めたとは扱わない。

## Verification Result

再現した3件の不具合を修正した。モデル性能・長期ドリフト率の比較実験は実施していない。
新規コピーはmanifest v2。旧v1を自動移行せず、従来のバイト一致で検査する。
Windowsの専用回帰CIを追加し、Linuxでは既存の全体テストに同じケースを含める。

## v2レコードの完全性確認

PR #507のレビューに基づき、各v2レコードの `checkout_sha256` を必須化した。
元hashとの集合が実ファイルから得られる完全なLF/CRLF形式と一致することも確認する。
欠損・空の不足・無関係なhash・元hashとの重複・バイナリへの余分なhashを単独検査で拒否する。

追加5ケースと既存コピー回帰の計92件が成功、WindowsではPOSIX権限1件をskipした。
契約改変の既存fixtureは、全hashを正しく更新した上でも契約本文の改変を拒否するよう整えた。
記録は `review-tests.log` と `review-test-retry.log`、集約した行coverageは254/259行（98.07%）で
`coverage-review-final.json` に保存した。上表の全体回帰・wheelは初回実装時点の結果で、
最終コミットの全体回帰・Windows回帰・配布buildは[PR #507](https://github.com/RNA4219/workflow-cookbook/pull/507)のCIで確認する。
