---
acceptance_id: AC-20260910-02
task_id: 20260910-02
intent_id: INT-WORKFLOW-EVOLUTION
owner: Codex
status: approved
reviewed_at: 2026-09-10
reviewed_by: Codex
approval_type: technical
release_approval_id: null
---

# Acceptance: 評価・取得・観測・再開

## Scope

[Task 20260910-02](../tasks/task-workflow-evolution-20260910.md) の4項目。
[利用手順](../workflow-evolution.md) と4つの契約に対応する。
Agent_tools版の既存部品を接続する。agent-taskstate/memx-resolver本体は変更していない。

## Acceptance Criteria

- [x] manifest、hash、対応数、予算、欠測、測定失敗、負の対照を検証して比較する。
- [x] Birdseye鮮度、原文fallback、UTF-8予算、必読不足、memx cache更新を検証する。
- [x] task/run/spanを接続し、独立検収とtool成否を分け、正式Evidence schemaを検証する。
- [x] 既存taskstate CAS、成果物照合、lease、結果不明時の明示判断、再開を検証する。
- [x] 全pytest、既存coverage 80%条件、型・lint、文書ゲートを通す。

## Evidence

workspaceの `research/workflow-evolution-20260910/` に生ログ・coverage・実行記録を保存する。

- 対象fixture試験: 139 passed。新規131件と既存runtime 8件。
- 最終全pytest: 889 passed。全体の行coverage 83.00%（8850/10663）、既存80%条件達成。
- 対象機能の行coverage 92.66%（694/749）、分岐85.80%（278/324）。除外や閾値緩和は追加していない。
- Ruff、mypy（123 source files）、15資料のMarkdown lint、必須文書ゲートと実plugin Task同期が成功。
- 7資料のBirdseye原文・要約を確認し、28nodeの連番とindex/hot世代、90日検査を確認した。
- wheel/sdist構築、リポジトリ外の隔離環境から4 CLIの起動が成功。wheelのPython 123ファイルとソースが一致。
- 実taskstate CLIのDB再接続とCAS競合、実memxのcache再利用と変更時更新を確認した。
- Evidenceの正式schema snapshotと出典hashは `tests/fixtures/agent-protocols/` に保存した。
- 初回全体試験は888 passed / 1 failed。追加連携試験のimport状態を復元して干渉を修正し、対象22件の成功を確認した。
- 次の全体試験でBirdseye連番の不備を検出して修正。最終の全テストは成功した。失敗ログも保持した。
- 最終ログはcompleted-tests.log、focused-final-tests.log、checks.json、wheel-verification.jsonを参照する。

## Verification Result

判定: この4項目の技術検証はapproved。公開・release承認を意味しない。

既存の文書41件のレビュー期限超過と旧capsuleの警告は残る。日付だけの延長や全索引の確認済み扱いはしていない。

実運用の比較評価、モデル成功率・速度・token削減は未測定。
外部モデルを起動するrunner・採点器、分散commit、外部副作用のexactly-onceは提供していない。
checkpointは既存state CLIと協調leaseを使う。結果不明の処理は自動再実行しない。
新機能は明示的にCLI/APIを使って接続する。全既存workflowの既定挙動を置き換えるものではない。
隣接repoがない単独CIでは連携2件がskipされる。今回のWindows/Python 3.11では両方実行した。

## 追加検証（2026-09-11）

未到達経路を検証する57ケースを追加し、4機能の行coverageを92.66%から100%（749/749）、
分岐coverageを85.80%から100%（324/324）へ引き上げた。対象196件、全pytest 946件が成功。
repo全体の行coverageは83.00%から83.51%（8905/10663）。対象範囲の100%と全体の値を区別する。

不正manifest/record、索引・capsule破損、予算境界、矛盾したtrace、実測0、保存状態の不整合、
復旧判断の拒否、確認後のlease失効、module CLIのJSON・終了コードを検証した。
本体123ファイルとpyproject.tomlの計124ファイルは開始前hashと一致。除外・閾値緩和は追加していない。
Ruff・format・mypyも成功。Windowsの全体試験は親子プロセスへPYTHONUTF8=1を継承して実行した。

証跡はworkspaceの `research/workflow-coverage-20260911/RESULTS.md`、`COMPLETION.json`、
`full-final-tests.log`、`focused-final.json` に保存した。以前の完了snapshotはそのまま保持する。
この追加検証もfixture試験であり、実運用の性能比較は実行していない。
