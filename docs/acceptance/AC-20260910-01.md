---
acceptance_id: AC-20260910-01
task_id: 20260910-01
intent_id: INT-POLICY-20260910
owner: Codex
status: approved
reviewed_at: 2026-09-10
reviewed_by: Codex
approval_type: technical
release_approval_id: null
---

# Acceptance: Workflow Policy Remediation

## Scope

[Task 20260910-01](../tasks/task-policy-remediation-20260910.md) に記載した100ルールの適用・実装・文書整合。
この技術検証はrelease承認、セキュリティ例外の再承認、実運用の効果証明を含まない。

## Acceptance Criteria

- [x] 全100 IDに適用判断・理由・参照・検証を記録する。
- [x] 行動方針、テンプレート、実装、変更対象のBirdseye要約を整合させる。
- [x] 該当テスト、全テストとcoverage 80%、lint、型検査を通す。
- [x] 配布物を構築し、一時環境でCLIを検証する。

## Evidence

| 検証 | 結果 |
| --- | --- |
| 全pytest | 675 passed |
| coverage（tools + security_headers） | 81.40%、既存80%条件達成 |
| Ruff / mypy | 成功 |
| 文書必須ゲート | front matter、Acceptance、CI matrix、Task同期、completion、境界・RUNBOOK検査が成功 |
| Birdseye | 変更対象11capsの原文・要約hashとreviewを確認、構造・90日検査成功 |
| 配布物 | wheel/sdist構築、隔離環境で5つの公開CLI起動が成功 |

workspaceの `research/workflow-policy-remediation-20260910/` に、正確なコマンド・終了コード・生ログ・各ID判断・初期／最終hashを保存。
`hub-pytest-final.log`、`hub-coverage-final.xml`、`hub-wheel-smoke.log` を参照する。

## Verification Result

判定: 本改修の技術検証はapproved。公開承認は別途必要。

- 全100件はルールの実装・維持確認・条件外判断であり、100個の不具合修正を意味しない。
- 文書レビュー期限検査は既存の41件超過によりexit 1（CIのwarning扱い）。期限だけを延長していない。
- 未移行の旧caps・directory nodeには鮮度不明の警告が残る。今回の確認対象を全索引へ拡張して報告しない。
- 実運用Gate効果、モデルtoken削減、90/180日の実測収集は未検証。fixture試験を効果の証明に使わない。
- Python 3.12・Linux/GitHub CIはこのWindows/Python 3.11検証とは別。remote実行・commit/push・release・通知は行っていない。

## 追加検証（2026-09-10）

83ケース（Gate観測46、共有workspace37）を追加し、全pytest 758件が成功。全体の行カバレッジは82.17%（8132/9897）。
Gate観測checkerの行は100%（89/89）、共有workspace調整処理の行は98.45%（318/323）。
対象を絞った分岐計測では、それぞれ100%（40/40）・94.05%（79/84）を確認した。

入力拒否、期間・件数の境界、欠測と実測0、CLI終了コードを検証した。
テスト追加に伴う実装・coverage対象・80%条件の変更はない。
上のEvidence表は100件改修完了時点の記録として保持する。
追加分のログ・coverage JSON/XMLはworkspaceの `research/workflow-policy-coverage-20260910/` を参照する。
単体fixture試験であり、実運用の比較評価・効果測定は実行していない。
