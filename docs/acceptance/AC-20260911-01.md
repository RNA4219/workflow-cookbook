---
acceptance_id: AC-20260911-01
task_id: 20260911-01
intent_id: INT-WORKFLOW-HATE-QEG
owner: Codex
status: approved
reviewed_at: 2026-09-11
reviewed_by: Codex
approval_type: technical
release_approval_id: null
---

# Acceptance: HATE・QEGによる実テスト受入

## Scope

[Task](../tasks/task-hate-qeg-20260911.md)と[接続仕様](../contracts/hate-qeg-test-gate.md)に対応。
Agent_tools版の全pytestを実行し、HATE 0.3.0で正規化・export、QEG 0.4.0で実行資格・判定・記録を検証する。
[実行手順](../workflow-evolution.md#hateqegで実行証跡を検証する)を追加した。

## Acceptance Criteria

- [x] 新しいJUnit/Coberturaを生成し、HATE全caseのidentityとstatusを一対一照合する。
- [x] 未commit変更を含むbuildを固定し、HATE/QEGの実行用コピーとともに前後のhash一致を確認する。
- [x] 全caseに加えpytest終了codeとcoverage条件をQEGへ渡し、結果欠落を隠さない。
- [x] QEGのvalidate、gate、record、outputs readがすべて終了0になる。
- [x] 失敗・skip・原本欠落・hash不一致の合成対照を実CLIで検証する。
- [x] 再実行できるCLI、仕様、検証原本、範囲と残件を保存する。

## Evidence

workspaceの `research/workflow-hate-qeg-20260911/run-002/` が最終実行。

| 確認 | 実測結果 | 原本 |
| --- | --- | --- |
| 全pytest | 976 passed、skipなし | pytest.stdout.log、raw/junit.xml |
| 全体の行coverage | 83.23%（9070/10897）、閾値80%維持 | raw/coverage-summary.json |
| 直近4機能の行coverage | 100%（749/749） | 同JSONの該当6ファイル |
| HATE正規化 | eligible、DQなし | hate/p0a/precheck-decision.json |
| HATE export | success、schema valid、partial=false | hate/export/qeg-export-report.json |
| QEG採用実行 | 977件すべてpass。976caseとpytest/coverageの集約判定1件 | qeg-gate.stdout.log |
| QEG最終判定 | Go、DQ 0、blocker 0 | quality-evidence-record.json |
| QEG各CLI | validate/gate/record/outputs readがすべて終了0 | 各command.json |
| source固定 | 対象コード・固定HATE・固定QEGの前後一致 | source-verification.json |

接続試験30件を追加した。実CLIを用いた合成試験は、成功→Go、失敗→No-Go、
skip/原本欠落/hash不一致→失格を確認している。製品の実976件とは別の成功件数として重複加算しない。
HATE/QEG本体への変更は行っていない。専用コピーのsource/Node依存はtoolchain配下へ保存した。

最初のrun-001は974 passedだったが、validateの事前期待値不足と並行作業中のツール変更を検出し、
接続全体をfailedと記録した。仕様に基づく期待値の事前保存と実行用コピーを追加し、run-002で再検証した。
過去のログは書き換えていない。

## Verification Result

このローカル自動テスト受入はapproved。QEGの実判定はGo。
今回増やした接続処理を含めた全体coverageであり、前回の83.51%とは分母が異なる。

モデルの実運用性能比較、RanD要求監査、Code-to-gate静的解析、手動BB、本番deploy、release承認は未評価。
HATEのflake履歴とmutation evidenceも未取得。coverageは集約値で、全行のcase別因果関係を証明するものではない。
既存CIへ常時実行する設定は変更せず、必要なruntimeを用意した環境でCLIを呼ぶ入口を提供する。
